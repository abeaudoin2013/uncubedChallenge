<div id="list-item" data-list-id="<%= list.id %>">

	<h3 class="list-item-title"><%= list.title %></h3>
	<p class="list-item-created-at"><%= list.created_at.strftime("%d %b. %Y") %></p>

	<%= link_to "COMPLETED!", list_path(list.id), method: :delete, remote: true, class: "delete-item" %> 

</div>
